package com.example.meal_planner_cookbook;

import java.util.List;

// tagging interface meaning the classes who implement it do not need to add extra code,
// however, they are both now the same type of object
public interface ApiResults {

}

